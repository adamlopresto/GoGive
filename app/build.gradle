apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion '26.0.2'

    defaultConfig {
        applicationId "fake.domain.adamlopresto.gogive"
        minSdkVersion 15
        targetSdkVersion 25
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
dependencies {
    compile 'com.google.android.gms:play-services-appindexing:9.8.0'
}

